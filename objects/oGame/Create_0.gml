randomize();
/*repeat(100) instance_create_layer(random_range(140,room_width-140),
								  random_range(300, room_height),
								  "Instances",
								  oPeg);
	

peg_count = 0;
gem_count = 0;

while (peg_count < 66 and gem_count < 33){
	x = random_range(140, room_width - 140);
	y = random_range(300, room_height);
	if (distance_to_object(oPeg) > 10) {
		if (distance_to_object(oGem) > 150 and gem_count < 33){
			instance_create_layer(x, y, "Instances", oGem);
			gem_count++;
		} else { 
			instance_create_layer(x, y, "Instances", oPeg);
			peg_count++;
		}
	}

}
*/
